public class MyClass {

    public static void main (String[] args){
        System.out.println("hello world");
        int x =3, y=4, z=6;
        System.out.println(x + y +z);
        int b = Math.max(x,y);
        System.out.println(b);
        String random = "sentence is one"; // so the gist is that the index of the first chartacter of the first occurring reference is called upon like i call is, "i" in is occurs at the 9th index as a result

        System.out.println(random.indexOf("is"));

    }
}
